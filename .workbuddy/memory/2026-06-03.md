# 2026-06-03 工作日志

## 除重/重 新增 nums 输入框
- cz_rows / cz_rows2 每行新增 nums 字段，默认值 '0123456789'
- HTML 模板：除重、重 各行加一个 input 绑定 cz_rows[ri].nums / cz_rows2[ri].nums
- killRepeat 方法更新：勾选位全部相同时，还需判断该重复数字是否在 nums 输入框中，才执行排除
- 新行 push 时附带 nums: '0123456789'
- localStorage 读取时补 nums 默认值（兼容旧缓存数据）
