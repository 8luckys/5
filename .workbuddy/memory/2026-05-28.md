# 2026-05-28 工作日志

## index.html Vue前端组件迭代

- 新增「除重」功能：参考合分除模式，模板用 v-for 循环渲染 cz_rows，每行仅有4个checkbox（无input），点击第一行「除重」标签追加新行
- 逻辑：killRepeat(i_arr) 方法，遍历 cz_rows，勾选位（checked>=2）中若存在重复数字则排除该号码
- 在 onSubmit 循环中加入 `if (this.killRepeat(i_arr)) continue;`
- cz_rows 已加入 localStorage 缓存（form_cache_v1）：读取、watch、保存三处均已配置

## Bug修复：SyntaxError Unexpected token 'this'

- 原因：插入 killRepeat 方法时，覆盖了 getShowColor(nums, i, index) 的函数声明头，导致其函数体变为孤立代码
- 修复：补回 getShowColor 函数声明，将 killRepeat 和 getShowColor 正确排列在 methods 中

## 新增：除重2（合分下面的副本）

- 复制除重功能放在合分（xwj_rows2）下方，命名为 cz_rows2
- 模板：v-for 渲染 cz_rows2，点击第一行「除重」追加新行，4个checkbox无input
- 逻辑：killRepeat2(i_arr) 方法，遍历 cz_rows2 判断重复
- onSubmit 中 xwj2_show 分支加入 killRepeat2 判断
- 缓存：cz_rows2 加入 localStorage（created读取、watch监听、_saveCache保存）
